# Agent instructions

Project rules for the AI agent live in **`.cursor/rules/`**. Start with **`follow-user-scope.mdc`**: implement only what the user asked for; do not add extra modules (e.g. `*-config.ts`) or features unless they explicitly request them.
